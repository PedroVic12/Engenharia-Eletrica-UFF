#include <array>
#include <iostream>

// faça um for loop com recursividade de programação funcional


// Classe Main
int main(int argc, char const *argv[])
{

    Escrever('');

    /* code */
    std::array<int, 5> x{{1, 2, 3, 4}};
    st
            std::cout
        << x[2] << '\n'
        << x.at(3) << "\n";
    std::cout << x.size() << '\n';
    std::cout << x.max_size() << '\n';
    std::cout << x.front() << "\n"
              << x.back() << "\n";

    return 0;
}

class meuScript
{
private:
    /* data */
public:
    meuScript(/* args */);
    ~meuScript();
};

meuScript::meuScript(/* args */)
{
}

meuScript::~meuScript()
{
}

// faça um hello world em c++
