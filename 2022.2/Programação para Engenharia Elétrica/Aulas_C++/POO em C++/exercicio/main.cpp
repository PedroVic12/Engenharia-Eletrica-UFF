/* 
1) Em um dispositivo eletrônico inteligente (IED) utilizado para proteção de SEP
são armazenados registros oscilográficos. Estes registros contem os valores
instantâneos de corrente e tensão lidos pelos seus sensores. Crie uma classe
fazendo um diagrama UML e código em C++ chamada oscilografia que permite registros
oscilográficos de tamanho variável para tensão e corrente monofásicas, e com a
data inicial de registro.

A) Crie um construtor que considera um tamanho de registro variável que pode
ser escolhido pelo usuário

B) Crie operador de cópia que faça a cópia (deep copy) de todos os atributos

C) Crie destrutor que libere  alocação dinâmica de todos os atributos

D) Crie métodos de acesso (getter) e mutantes (setter) que acessam um elemento
do registro ou todo o registro
    
E) Usar epoch(epochconverter.com) para representar a data inicial, considerando ms
*/