// Crie um programa que recebe um vetor de n elementos d usuário via teclado e imprime na tela

#include <iostream>

using namespace std;

// Declarar o vetor, como?
int numero;

int main(int argc, char const *argv[])
{
	cout << "Ola Mundo!" << endl;
	cout << "Digite um número inteiro  " << endl;
	cin >> numero;
	cout << "O numero digitado é " << numero << endl;

	for (int i = 0; i < numero; i++)
	{
		// preencher o vetor no loop
	}

	for (int i = 0; i < numero; i++)
	{
		// Saida de dados pelo loop
	}

	return 0;
}
